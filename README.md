# fetchstore-app
A simple frontend eCommerce application using Fetch API with login, product listing, and product detail pages. Built with vanilla JavaScript and structured using Git workflow (main, develop, feature branches).
